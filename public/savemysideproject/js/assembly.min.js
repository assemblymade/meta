$(document).ready(function() {
    function a() {
        var a = $(this).scrollTop();
        Math.abs(d - a) <= e || (a > d && a > f ? ($('header.global').addClass('nav--hide'), $('body').removeClass('nav--open')) : a + $(window).height() < $(document).height() && $('header.global').removeClass('nav--hide'), d = a)
    }
    var b = $(window).height();
    $('.nav_trigger').click(function() {
        $('body').toggleClass('nav--open')
    }), $('.story_block').css('height', b), $(window).resize(function() {
        var a = $(window).height();
        $('.story_block').css('height', a)
    });
    var c, d = 0,
        e = 5,
        f = $('header.global').outerHeight();
    $(window).scroll(function() {
        c = !0, $('header.global').toggleClass('not_top', $(window).scrollTop() >= $('.hero .content').height()), $('.the_story').toggleClass('stick_it', $(window).scrollTop() >= $('.hero .content').height()), $('.the_story').toggleClass('too_far', $(window).scrollTop() >= $('.ship').offset().top), $('.idea .story_graphic').toggleClass('go', $(window).scrollTop() >= $('.idea').offset().top - 150), $('.rally .story_graphic').toggleClass('go', $(window).scrollTop() >= $('.rally').offset().top - 150), $('.ship .the_product').toggleClass('go', $(window).scrollTop() >= $('.ship').offset().top - 150), $('.ship .success').toggleClass('go', $(window).scrollTop() >= $('.ship').offset().top - 150)
    }), setInterval(function() {
        c && (a(), c = !1)
    }, 250), $('.popup-video').magnificPopup({
        disableOn: 700,
        type: 'iframe',
        mainClass: 'mfp-fade',
        removalDelay: 160,
        preloader: !1,
        fixedContentPos: !1
    })
}), $(window).load(function() {
    var a = $('.community_grid');
    a.masonry({
        gutter: 20
    })
});
