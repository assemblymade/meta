<% title 'Proposals' %>
<% activate_nav! :people %>
<% activate_nav! :people_governance %>

<%= render 'people/nav' %>

<div class="sheet-body">
  <div class="row">
    <div class="col-md-12 col-xs-12">
      <%= react_component('ProposalEdit', newsFeedItem: @proposal.news_feed_item, user: UserSerializer.new(@proposal.user), proposal: ProposalSerializer.new(@proposal)) %>
    </div>
  </div>
</div>
