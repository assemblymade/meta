<%= initial_store_data('ProductFollowersStore',
        products: [{ id: store_data(:product).id, followers_count: store_data(:product).watchings_count }],
        userFollowings: (store_data(:product).watching?(current_user) ? [store_data(:product).id] : [])
) %>
