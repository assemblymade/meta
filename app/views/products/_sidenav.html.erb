<%= render 'products/launch' unless product.launched? %>

<% if current_user && product.core_team?(current_user) %>
  <%= form_for :asset, url: product_logos_path(product), html: {id: 'product-logo-form'} do |f| %>
    <%= f.hidden_field :attachment_id %>
    <%= f.hidden_field :name %>
  <% end %>
  <div class="js-dropzone"
      data-target-form="#product-logo-form"
      data-react-class="DragAndDrop"
      data-react-props="<%= {
        url: product.poster_image.url,
        alt: product.name
      }.to_json %>">
  </div>
<% else %>
  <span class="img-shadow">
    <%= image_tag product.poster_image.url, class: 'img-rounded', width: '100%', alt: product.name %>
  </span>
<% end %>

<% if @product.homepage_url.present? %>
  <a class="small truncate text-muted text-center" style="display:inline-block;width: 100%;margin-top:5px" href="<%= @product.homepage_url %>" data-track="product.site.clicked">
    <strong><%= @product.homepage_url %></strong>
  </a>
<% end %>

<ul class="nav nav-stacked nav-sidebar" style="margin-top: 35px;">
<li class="<%= nav_active_class(:about) %>">
  <a href="<%= product_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-eyeglasses dropdown-glyph"></i>
    Plan
  </a>
</li>

<li class="<%= nav_active_class(:chat) %>">
  <a href="<%= product_chat_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-bubbles dropdown-glyph"></i>
    Chat
  </a>
</li>

<% if can?(:update, product) || product.posts.any? %>
  <li class="<%= nav_active_class(:blog) %>">
    <a href="<%= product_posts_path(product) %>">
      <div class="pull-right" style="white-space:nowrap">
        <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
        <span class="sr-only">More info</span>
      </div>

      <i class="icon-notebook dropdown-glyph"></i>
      Journal
    </a>
  </li>
<% end %>
</ul>

<h6 style="padding-left:10px">Work</h6>

<ul class="nav nav-stacked nav-sidebar">

<li class="<%= nav_active_class(:projects) %>">
  <a href="<%= product_milestones_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-folder dropdown-glyph"></i>
    Projects
  </a>
</li>

<li class="<%= nav_active_class(:bounties) %>">
  <a href="<%= product_wips_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-trophy dropdown-glyph"></i>
    Bounties
  </a>
</li>

<li class="<%= nav_active_class(:discussions) %>">
  <a href="<%= product_discussions_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-book-open dropdown-glyph"></i>
    Discussions
  </a>
</li>

<li class="<%= nav_active_class(:assets) %>">
  <a href="<%= product_assets_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-layers dropdown-glyph"></i>
    Assets
  </a>
</li>

<li class="<%= nav_active_class(:repositories) %>">
  <a href="<%= product_repos_path(product) %>">
    <i class="icon icon-repository dropdown-glyph"></i>
    Repositories
  </a>
</li>
</ul>

<h6 style="padding-left:10px">People</h6>

<ul class="nav nav-stacked nav-sidebar">

<li class="<%= nav_active_class(:core_team) %>">
  <a href="<%= product_core_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <span class="icon-user-following dropdown-glyph"></span>
    Core Team
  </a>
</li>

<li class="<%= nav_active_class(:people) %>">
  <a href="<%= product_people_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <span class="icon icon-users dropdown-glyph"></span>
    Teams
  </a>

</li>

<li class="<%= nav_active_class(:ownership) %>">
  <a href="<%= product_partners_path(product) %>">
    <div class="pull-right" style="white-space:nowrap">
      <span class="icon icon-info text-light link-hover-primary" data-container="body" data-toggle="popover" data-placement="right" data-trigger="hover" data-content="From elevator pitch to monetization strategies, the Plan should answer questions about your business."></span>
      <span class="sr-only">More info</span>
    </div>

    <i class="icon-pie-chart dropdown-glyph"></i>
    Ownership
  </a>
</li>
</ul>

<% if product.revenue? %>
<h6 style="padding-left:10px">Financials</h6>

<ul class="nav nav-stacked nav-sidebar">

  <li class="<%= nav_active_class(:financials) %>">
    <a href="<%= product_financials_path(product) %>">
      <i class="icon icon-wallet dropdown-glyph"></i>
      Money
    </a>
  </li>

  <!-- <li class="<%= nav_active_class(:contracts) %>">
    <a href="<%= product_contracts_path(product) %>">
      <i class="icon icon-docs dropdown-glyph"></i>
      Contracts
    </a>
  </li> -->
</ul>
<% end %>

<% if staff? %>
  <div class="panel panel-danger">
    <div class="panel-heading">
      <h6 class="panel-title">DANGER</h6>
    </div>
    <div class="panel-body">
      <% if @product.flagged? %>
        <span class="btn btn-danger btn-sm btn-block btn-disabled" href="#" style="margin-bottom: 16px;">Product Flagged</span>
      <% else %>
        <a class="btn btn-danger btn-sm btn-block" style="margin-bottom: 8px;" href="<%= product_flag_path(@product)%>">Flag Product</a>
      <% end %>

      <%= form_for [:admin, @product.showcases.new], url: admin_staff_picks_path do |f| %>
        <%= f.hidden_field :product_id %>
        <%= f.submit "Schedule Staff Pick", class: %w(btn btn-default btn-sm btn-block) %>
      <% end %>
    </div>
  </div>
<% end %>