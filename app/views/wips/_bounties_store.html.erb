<% if @wips && @wips.any? %>
  <%= initial_store_data(
    'BountiesStore',
    PaginationSerializer.new(
      @wips.try(:page, 1),
      each_serializer: BountyListSerializer,
      root: :bounties
    ).as_json
  ) %>
<% end %>
