<% if @bounties && @bounties.any? %>
  <%= initial_store_data(
    'BountiesStore',
    PaginationSerializer.new(
      @bounties.try(:page, 1),
      each_serializer: BountyListSerializer,
      root: :bounties
    ).as_json
  ) %>
<% end %>
