<% title @faq_group.name, 'FAQ' %>
<% provide(:description, "Frequently asked questions about #{@faq_group.name.downcase} on Assembly.") %>

<!-- TODO Deprecate. Pages now use gray bg. (chrislloyd) -->
<style>body { background-color: white !important; }</style>

<% activate_nav! :platform_faq %>
<%= render 'shared/navbar' %>

<%# cache FaqGroup.cache_key(@faq_group) do %>
  <div class="container">

    <div class="border-bottom py3 mb4">
      <ol class="breadcrumb mb2">
        <li class="active">FAQ</li>
        <li></li>
      </ol>
      <h1 class="mt2 mb0"><%= @faq_group.name %></h1>
    </div>

    <div class="clearfix mxn4">
      <div class="md-col md-col-8 px4">

            <ul class="list-reset">
              <% @faq_group.questions.each do |question| %>
                <li class="question">
                  <%= link_to question[:name], anchor: question[:anchor] %>
                </li>
              <% end %>
            </ul>

            <hr class="border-bottom mt4 mb4">

            <%= @faq_group.body.html_safe %>

      </div>

      <div class="md-col md-col-4 px4">
        <ul class="list-reset">
          <% @faq_groups.each do |faq_group| %>
            <li>
              <a class="block gray-1 py1 <%= 'bold blue' if @faq_group.slug == faq_group.slug %>" href="<%= help_path(faq_group) %>"><%= faq_group.name %></a>
            </li>
          <% end %>
        </ul>
      </div>

    </div>
  </div>
<%# end %>

<%= render 'shared/footer' %>
