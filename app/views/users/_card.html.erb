<div class="bg-white border rounded mb2">
  <div class="center p3">

    <div class="mx-auto mb2" style="width:120px">
      <%= avatar_tag(user, 120) %>
    </div>

    <h3 class="black mt0 mb0"><strong><%= user.username %></strong></h3>

    <% if user.location? %>
      <div class="h6 gray-dark mt0 mb0"><%= user.location %></div>
    <% end %>
  </div>

  <% if user.bio? %>
    <div class="p3 center gray-darker border-top">
      <%= react_component 'Markdown', content: markdown(user.bio), normalized: true %>
    </div>
  <% end %>

  <div class="h6 mt0 mb0 center">

    <!-- Staff -->
    <% if user.staff? %>
      <a class="block px3 py2 border-top gray-dark" href="<%= product_people_path(product_id: 'meta', filter: 'core') %>" title="Assembly Staff">
        <%= inline_react_component 'Icon', icon: 'coffee' %>
        Assembly Staff
      </a>
    <% end %>

    <!-- Twitter -->
    <% if user.twitter_connected? %>
      <a class="block px3 py2 border-top gray-dark" href="<%= user.twitter_url %>" title="@<%= @user.twitter_nickname %>">
        <%= inline_react_component 'Icon', icon: 'twitter' %>
        <%= user.twitter_nickname %>
      </a>
    <% elsif signed_in? && @user == current_user %>
      <a class="block px3 py2 border-top gray-dark" href="<%= user_omniauth_authorize_path(:twitter) %>">
        <%= inline_react_component 'Icon', icon: 'twitter' %>
        Connect Twitter account
      </a>
    <% end %>

    <!-- Github -->
    <% if user.github_connected? %>
      <a class="block px3 py2 border-top gray-dark" href="<%= user.github_url %>" title="<%= @user.github_login %>">
        <%= inline_react_component 'Icon', icon: 'github' %>
        <%= @user.github_login %>
      </a>
    <% elsif signed_in? && @user == current_user %>
      <a class="block px3 py2 border-top gray-dark" href="<%= user_omniauth_authorize_path(:github) %>">
        <%= inline_react_component 'Icon', icon: 'github' %>
        Connect GitHub account
      </a>
    <% end %>
  </div>
</div>
