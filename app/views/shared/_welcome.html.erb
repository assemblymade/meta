<% if (current_user && current_user.welcome_banner_dismissed_at.nil?) && cookies['dismissed-welcome'].nil? %>
  <div data-react-class="WelcomeBanner"
      data-react-props="<%= {
        discoverBountiesPath: discover_path(:bounties),
        userPath: (dismiss_welcome_banner_path(current_user) if current_user)
      }.to_json %>">
  </div>
<% end %>
