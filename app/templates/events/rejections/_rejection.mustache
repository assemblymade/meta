<div class="timeline-item">
  <div class="media">
    <div class="pull-left">
      <div class="marker green border-green">
        <span class="icon icon-circle"></span>
      </div>
    </div>

    <div class="media-body">
      <div class="media-heading">
        Re-opened by
        {{#actor}}
          <a href="{{url}}">{{username}}</a>
        {{/actor}}
      </div>

      <div class="markdown-normalized">{{{body_html}}}</div>
    </div>
  </div>
</div>
