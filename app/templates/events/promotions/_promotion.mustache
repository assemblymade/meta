<div class="activity">

  <div class="pull-left">
    <div class="marker purple border-purple">
      <span class="icon icon-double-chevron-up"></span>
    </div>
  </div>

  <div class="activity-body">
    <div class="activity-actions text-link-bold">
      Promoted by
      {{#actor}}
        <a href="{{url}}">{{username}}</a>
      {{/actor}}

      <ul class="list-inline pull-right mb0">

        <li>
          <div class="dropdown">
            <a href="#" class="dropdown-toggle" id="dropdown-{{cid}}" data-toggle="dropdown">
              <i class="icon icon-ellipsis" style="font-size: 18px;"></i>
            </a>
            <ul class="dropdown-menu pull-right text-small" role="menu" aria-labelledby="dropdown-{{cid}}">
              {{#edit_url}}
                <li>
                  <a href="{{edit_url}}">
                    <i class="icon-pencil mr1"></i>
                    Edit comment
                  </a>
                </li>
              {{/edit_url}}

              {{#anchor}}
                <li>
                  <a href="#{{anchor}}">
                    <i class="icon-link mr1"></i>
                    Permalink
                  </a>
                </li>
              {{/anchor}}
            </ul>
          </div>
        </li>

      </ul>
    </div>

    <div class="activity-content markdown-normalized">{{{body_html}}}</div>

  </div><!-- .activity-body -->

</div><!-- .media -->
